Sample Excel fixtures for import testing.

- `sample-plan.xlsx` — export of the demo seed plan (RU headers + optional id/dates).

Required columns: `задача`, `описание`, `исполнитель`, `длительность`, `предшественники`.
